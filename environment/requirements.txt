# Python dependencies for scripts 06, 08, 09-13
# Install with:  pip install -r environment/requirements.txt
#
# Versions below are lower bounds matching those used for the manuscript
# (see environment/versions.md for the exact versions).

pandas>=2.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.8
adjustText>=1.0
